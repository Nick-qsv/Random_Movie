//set up CSS for header
//X
//set up CSS for Button
//X

//set up CSS for MovieList
//X

//connect the database
//X

//every time the button is presssed add an entry with the Name and the Rating

//set up a POST route

//set up a Store that updates the state, has an action creator and reducer for this adding of a movie then calls this post route.

//write JSX to render the movie list from the database

//successfully post new movies to the list